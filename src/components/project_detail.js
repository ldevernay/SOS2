import React, { Component } from 'react';

export default () => {
    if (!this.props.project) {
        return (
            <div>
                Sélectionnez un projet pour commencer
            </div>
        );
    } else {

    }
}